// This root-level source file is intentionally tiny. cgo only compiles native
// source files that live directly in the Go package directory, so it includes
// the real implementation from shim/.
#include "shim/desktopdup_shim.cpp"
